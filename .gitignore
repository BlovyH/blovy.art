node_modules
dist
dist-ssr
*.local
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# temp: screenshots / generated images
screenshot*.png
*.png.bak

# generated by build (font subsetting) — regenerated on every dev/build/CI run
public/assets/fonts/*-subset.woff2

# gallery 原图与脚本生成产物（原图本地保留，WebP 上传 R2；均不进仓库）
assets/gallery-originals/
assets/gallery-dist/

.aseprite
.env.development
# wrangler 含 account_id 隐私，真文件不进仓库；模板见 wrangler.toml.example
wrangler.toml
# wrangler 本地 dev 状态（本地 R2/KV 模拟、cache），纯本地再生，不进仓库
.wrangler/

# json 生产直接读远端的，这个只是本地测试用
public/data/content.json
